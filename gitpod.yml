# .gitpod.yml

# Tasks to run when the Gitpod environment is initialized
tasks:
  - name: Install Python Dependencies
    command: pip install pytest selenium pytest-xdist

git:
  global:
    user.name: "Pranav Naik"
    user.email: "nk.pranav99@gmail.com"
